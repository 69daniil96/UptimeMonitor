# config.py — Настройки мониторинга
# ============================================================
# Этот файл содержит все параметры, которые можно менять
# без правки основного кода: список хостов, таймауты и т.д.
# ============================================================

# Хосты для мониторинга.
# Каждый хост — словарь с тремя полями:
#   name — человекочитаемое имя (отображается в таблице)
#   host — IP-адрес или доменное имя
#   port — TCP-порт для проверки (53 = DNS, 443 = HTTPS, 80 = HTTP)
HOSTS = [
    {"name": "Google DNS",   "host": "8.8.8.8",       "port": 53},
    {"name": "Cloudflare",   "host": "1.1.1.1",       "port": 53},
    {"name": "GitHub",       "host": "github.com",     "port": 443},
    {"name": "Yandex",       "host": "ya.ru",          "port": 443},
    {"name": "Localhost",    "host": "127.0.0.1",      "port": 80},
]

# Интервал между циклами проверки (в секундах).
# Воркер проверяет ВСЕ хосты, ждёт CHECK_INTERVAL секунд, повторяет.
CHECK_INTERVAL = 3

# Таймаут подключения к одному хосту (в секундах).
# Если за это время сокет не установил соединение — хост считается оффлайн.
TIMEOUT = 2
